# AGENTS.md

## 基本沟通

1. 始终使用中文与我沟通，除非代码、命令、日志、报错信息本身需要保留英文原文。
2. 回答要直接、具体。最终总结只口述说明本次修改内容、验证结果、遗留风险和下一步建议，不要额外生成一堆 Markdown 文档。
3. 除非我明确要求，项目内只维护一个 `README.md` 作为说明和更新记录，不新增其他总结类 `.md` 文件。

## 项目背景

1. 当前项目是无人机比赛任务开发项目比赛规则在/home/orangepi/catkin_ws/src/D题_陆空协同无人机系统.pdf，我们现在完善代码进行完成任务，当前工程中ego_api有一个写过的比赛代码是另一个比赛的你可以作为参考，但更多是使用这里面通过ego_bridge封装的控制函数
2. 修改时优先保证现有任务流程稳定，不为了重构而重构，不引入与比赛任务无关的大改动。

## 接手任务时的要求

1. 在开始任务前，优先快速检查是否有适合当前任务的 Skill，插件工作流；如果有，应说明使用哪个流程以及原因。
2. 如果是在已有代码基础上更新功能、修复问题或优化逻辑，必须先了解仓库结构和现有流程。
3. 优先阅读 `README.md`、项目入口文件、主要配置文件、启动脚本、任务流程相关代码。
4. 在没有理解现有逻辑前，不要直接改核心流程。
5. 如果需求不明确，先用中文确认目标、输入输出、运行环境、预期效果和不能破坏的功能。

## 修改代码时的要求

1. 遵循项目已有代码风格、命名方式、目录结构和运行方式。
2. 不要擅自删除、覆盖或回滚我已有的改动。
3. 对关键逻辑修改，应说明修改前后的流程差异和可能影响的模块。

## 验证与排错

1. 每次修改完成后，必须检查代码是否存在语法错误、运行错误、导入错误、路径错误、配置错误和明显逻辑冲突。
2. 必须尽可能运行与本次修改相关的测试、脚本、启动命令或静态检查。
3. 如果具备完整运行条件，需要验证全流程可执行性，并说明完整流程如何执行。
4. 如果因为缺少硬件、仿真环境、数据集、依赖、权限或外部服务导致无法完整验证，必须明确说明哪些部分已验证、哪些部分未验证、风险在哪里。
5. 遇到报错时，先定位根因，再修改；不要只根据表面报错盲目改代码。

## 最终汇报

每次完成修改后，用中文说明：

1. 本次改了哪些内容。
2. 涉及哪些文件或模块。
3. 当前完整流程应该如何执行。
4. 已经做了哪些验证，验证结果是什么。
5. 是否还存在未验证部分、潜在风险或需要我确认的事项。
6. 是否已经提交 Git，提交到了哪个仓库、哪个分支、提交信息是什么。

## Git 提交要求

1. 每次代码修改完成并通过必要检查后，需要提交到 Git，方便恢复。
2. 提交前必须检查当前目录下涉及几个 Git 仓库；如果项目由多个仓库组成，需要分别检查每个仓库的状态。
3. 只提交本次任务相关的改动，不提交无关文件、临时文件、缓存文件、日志文件或我已有但与你本次任务无关的改动。
4. 提交前需要查看 `git status` 和变更摘要，确认没有误提交。
5. 每个仓库需要单独提交，提交信息要简洁说明本次修改目的。
6. 默认只 commit，不 push；除非我明确要求推送远程分支。
